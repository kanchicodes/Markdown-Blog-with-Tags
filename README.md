# MarkNote

Minimal Markdown blog app using React, Vite, Tailwind.

Install:

```bash
npm install
npm run dev
```

Features: write markdown, live preview, tags, search, export, drafts, publish toggle.
